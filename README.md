# dotfiles

## Installation:

On a MacOS, Arch-based distribution, or Ubuntu distribution, run the following as root:

```bash
curl -LO dennisfarmer.dev/dennisfj.sh
sh dennisfj.sh
```

## TODO:
- sync with greatlakes, run jupyter notebook inside tmux so if vpn disconnects I can resume without having to restart training / etc.
